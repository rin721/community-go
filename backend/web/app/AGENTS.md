# Agent Rules：React WebUI 入口说明

`backend/web/app` 的 React 前端长期规则统一维护在聚合仓库根目录 `AGENTS.md`。

在当前 `banyao-web` 仓库内开发后台 React WebUI 时，必须阅读并遵守根 `AGENTS.md` 的“后台 React 前端”和“前端视觉与组件体系”章节。本文件用于指向统一规则入口，不承载独立规则。
