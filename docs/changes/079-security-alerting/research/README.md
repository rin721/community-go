# 079 研究档案：安全告警与动态风险控制

## 研究范围

承接 R077-002 判定（异常告警边界内可落地；动态风险控制需先设计研究），本档案回答：

1. **R079-001**：安全告警最小版如何在既有审计/业务边界上落地——事件源、规则集、通知窄 port、webhook provider、配置与失败语义。
2. **R079-002**：动态风险控制（异常登录/设备/IP 风险）的因子、数据源、与 MFA/告警的联动及实施边界（设计研究，本批不实施）。

## 检索方式

- 按 `docs/changes/README.md` 确认下一个变更序号为 `079`；工作树 clean（commit `6ae9e12`，078 完成）。
- 复用：`R077-002`（异常告警/风险控制判定）、`065`（低敏审计实现）、`078`（MFA/登录两步）、`077`（登录限流）、`064`（候选记录）。
- 代码证据：`internal/module/{iam,auth}` 的审计与登录/写操作边界、`pkg/httpx` 限流、`api/openapi.yaml`；快照 commit `6ae9e12`。
- 外部事实：IP 地理/信誉需外部数据（GeoLite 等），本批不引入；webhook 用标准库 HTTP 即可（零依赖）。

## 记录索引

| ID | 标题 | 状态 |
| --- | --- | --- |
| [R079-001](R079-001-alerting-minimal-design/report.md) | 安全告警最小版：事件源、规则集、通知 port、webhook 与配置 | active |
| [R079-002](R079-002-risk-control-design/report.md) | 动态风险控制设计：因子、数据源、与 MFA/告警联动与边界 | active |