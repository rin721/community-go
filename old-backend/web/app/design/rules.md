# React Design Rules 已合并

`backend/web/app` 的长期视觉、组件和可访问性规则已经合并到聚合仓库根目录 `AGENTS.md`。

在当前 `banyao-web` 仓库内开发后台 React WebUI 时，必须阅读并遵守根 `AGENTS.md` 的“后台 React 前端”和“前端视觉与组件体系”章节。本文件只保留为上游路径兼容提示，不再承载独立规则。
