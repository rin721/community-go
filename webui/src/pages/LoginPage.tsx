import { useState } from "react";
import { useNavigate } from "react-router-dom";
import { login, type AdminSession } from "../api";
export function LoginPage({ onSession }: { onSession: (value: AdminSession) => void }) { const [username, setUsername] = useState(""); const [password, setPassword] = useState(""); const [error, setError] = useState(""); const navigate = useNavigate(); return <form className="auth-card" onSubmit={(event) => { event.preventDefault(); login(username, password).then((value) => { onSession(value); navigate("/dashboard"); }).catch((reason: Error) => setError(reason.message)); }}><h1>登录 Admin</h1><label>用户名<input value={username} onChange={(event) => setUsername(event.target.value)} autoComplete="username" /></label><label>密码<input type="password" value={password} onChange={(event) => setPassword(event.target.value)} autoComplete="current-password" /></label>{error && <p className="error">{error}</p>}<button type="submit">登录</button></form>; }
