import { useState } from "react";
import { useNavigate } from "react-router-dom";
import { setup, type AdminSession } from "../api";
export function SetupPage({ onSession }: { onSession: (value: AdminSession) => void }) { const [token, setToken] = useState(""); const [username, setUsername] = useState(""); const [password, setPassword] = useState(""); const [error, setError] = useState(""); const navigate = useNavigate(); return <form className="auth-card" onSubmit={(event) => { event.preventDefault(); setup(token, username, password).then((value) => { onSession(value); navigate("/dashboard"); }).catch((reason: Error) => setError(reason.message)); }}><h1>首次设置</h1><p>完成后 setup 入口会永久关闭。</p><label>Setup Token<input value={token} onChange={(event) => setToken(event.target.value)} /></label><label>用户名<input value={username} onChange={(event) => setUsername(event.target.value)} /></label><label>密码<input type="password" value={password} onChange={(event) => setPassword(event.target.value)} /></label>{error && <p className="error">{error}</p>}<button type="submit">创建管理员</button></form>; }
